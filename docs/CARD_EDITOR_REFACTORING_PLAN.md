# CardEditor.vue分割計画

更新日: 2026-09-16

## 目的と進め方

`app/components/CardEditor.vue`（開始時3,993行）の責務を分割し、編集内容・履歴・保存形式・画面操作の互換性を維持する。
既存動作を先にテストで固定し、そのテストを維持しながら小さく抽出する、TDDに近い進め方を採用する。

各工程は次の順序で行う。

1. 対象の既存動作と失敗時の期待結果を列挙する。
2. 分割前の実装に対して回帰テストを追加し、正常に通ることを確認する。
3. テストが意図した退行を検出できることを、小さな一時変更で確認して元に戻す。
4. 振る舞いを変えずに責務を抽出する。必要なら抽出先の単体テストも追加する。
5. 対象テスト、全テスト、型チェック、lint、ビルドを実行する。
6. 結果と未完了項目をこの文書へ記録する。

テストが落ちたら次の抽出を止め、環境差・テストの不備・実装の退行・既存不具合を切り分ける。
アサーションの削除、skip、例外の握りつぶしで通さない。仕様判断が必要な場合は確認し、確定するまで依存する変更を進めない。
リファクタリングとは独立した不具合修正は、原因・変更理由・検証を区別して記録する。

## 状態と依存の境界

- 保存するデータ: `useProjectStore`。
- カード編集とUndo/Redo: `useCardEditor`。
- 画像・フォント・Object URL等の資源: `useProjectRuntime`。
- 機能内の一時状態と非同期処理: 各composable。
- 機能間の接続と画面構成: `CardEditor.vue`。

composable同士の循環依存や、全状態を渡す巨大なcontextは作らない。
必要な参照・操作のみを渡す。外部I/Oは差し替え可能にし、連携テストでは実際のストアと履歴を使う。
既存サービスやutilsの単体テストを複製せず、画面内に残る連携と失敗時の整合性を補う。

## 工程と受け入れ条件

### 0. 基準とテスト環境

- [x] 指定のNode 24.15.0で既存テストを確認する。
- [x] Vue SFCとDOMを扱えるコンポーネントテスト環境を追加する。
- [x] 外部I/O・重いCanvas描画を境界で置き換え、画面のイベント接続をテストする。

### 1. 描画遅延 — usePreviewDeferral

- [x] 分割前の画面で、IME変換中の待機、入力後500ms、再入力による延期、明示的解除をテストする。
- [x] 領域・表示画面・インスペクタータブ・画像の変更による解除をテストする。
- [x] 画面終了時のタイマー解放をテストする。
- [x] 状態、タイマー、監視、後処理を抽出して同じ回帰テストを通す。

### 2. サムネイル — useCardThumbnails

- [x] 同時実行数2、重複要求防止、キャッシュ破損時の再生成をテストする。
- [x] プロジェクト変更・リセット後の古い結果を反映しないことをテストする。
- [x] 読込失敗後もキューが進むことをテストする。
- [x] キューと世代管理を抽出し、資源所有はruntimeに残す。

### 3. 翻訳 — useEditorTranslation / useTranslationReview

- [x] 候補取得後のカード変更・領域削除・原文変更・訳文変更で上書きしないことをテストする。
- [x] 候補破棄ではデータと履歴を変更しないことをテストする。
- [x] CSV・レビュー反映時の編集中カードのUndoと他カードの更新をテストする。
- [x] 要求・候補適用を`useEditorTranslation`へ抽出する。
- [x] レビュー・CSV連携を抽出する。

### 4. OCR・共有資源

- [x] 単一領域OCRの候補確認・補正辞書・Undo/Redo・非同期結果の拒否をテストする。
- [x] 単一領域OCRを`useEditorOCR`へ抽出する。
- [x] OCR候補の選択・編集・確定と、複数候補を1回のUndoで戻す操作をテストする。
- [x] 一括OCRの中止時に完了済み候補が残り、未処理カードを開始しないことをテストする。
- [x] OCR失敗と画面終了時のWorker解放をテストする。
- [x] 領域候補の検出・編集と`useBatchOCR`を分離する。
- [ ] アセット・フォント変更時の共有参照、履歴、保存待ち、キャッシュの整合性をテストする。
- [x] `useEditorAssets` / `useEditorFonts`を抽出する。

### 5. プロジェクト — useProjectSession / useProjectPersistence

- [x] 保存失敗時に未保存状態・保存待ち画像・削除予定を保持することをテストする。
- [x] 保存済みの変更だけを承認し、保存開始後の変更を保持することをテストする。
- [x] 初回保存でカードIDを付与してもUndo履歴が残ることをテストする。
- [ ] カード画像読込失敗時の元カード保持、OCR・翻訳中の切替制限をテストする。
- [ ] 開く・切替と保存を抽出する。
- [x] 画像の一括書き出しで削除予定を除外し、終了時に元のカードへ戻すことをテストし、配置先を判断する。

### 6. 表示コンポーネント

- [ ] `EditorInspectorPanel.vue`: タブ、領域一覧、各インスペクターの配置を分離する。
- [ ] `DiagnosticsDialog.vue`: ログの表示・消去を分離する。
- [ ] `EditorConfirmDialog.vue`: 削除確認の表示を共通化し、削除処理は機能側に残す。
- [ ] ダイアログ・入力欄・処理中のUndoショートカット抑制を回帰テストする。
- [ ] props/emitsの単なる大量中継になっていないか確認する。
- [ ] 実ブラウザでIME、タブ操作、ダイアログ、Canvasの表示追従を確認する。

## 検証コマンド

```bash
mise exec -- pnpm test
mise exec -- pnpm typecheck
mise exec -- pnpm lint
mise exec -- pnpm build
git diff --check
```

DOMテストでは実際のブラウザ描画・日本語IME・File System Access APIは保証できない。
関連する実ブラウザ確認は `MANUAL_TESTS.md` と併せて記録し、未実施を成功扱いにしない。

## 実施記録

### 2026-09-16: 着手時の基準

- Node 24.15.0（mise）: 52ファイル、314テスト成功、未処理例外なし。
- 前回Node 22.17.1で発生した`Event.returnValue`の未処理例外は指定環境では再現しない。既存テストの変更は行わない。
- 最初の対象は工程0〜1。以降も工程ごとにテストを先行させる。

### 2026-09-16: 工程0〜1

- `@vitejs/plugin-vue`と`happy-dom`を開発依存に追加。既存テストはNode環境のまま、画面テストだけDOM環境を指定する。
- `tests/components/CardEditor.test.ts`に7テストを追加。分割前に全件成功を確認した。
- テストは子コンポーネントのイベントとCanvasへ渡すpropsを検証する。親の非公開関数の公開やソース文字列による検査は行わない。
- Pinia、`useCardEditor`、`useProjectRuntime`は実装を使用。Nuxtの自動importを補い、子画面・離脱ガード・OCR Workerは置き換える。離脱ガード自体は既存の単体テストが担当する。
- 検出力確認: 分割前の待機時間を一時的に500msから0msへ変えると、意図した2テストが失敗した。変更を戻してから抽出した。
- `usePreviewDeferral`へ状態・タイマー・watch・終了処理を移動し、同じ7テストが成功した。
- 画面テスト導入中の失敗は、ルーターのモジュール解決、スタブのコンポーネント名、既に選択済みのタブをクリックするテスト準備に起因。対象外の離脱ガードを境界で置き換え、スタブ名と切替先タブを修正した。製品の仕様変更はない。
- テストの型検査はSFC importに対応する既存依存の`vue-tsc`へ変更した。`tsc`では`.vue`の型を解決できなかったためで、型チェックを省略するshimは追加していない。
- 検証: Node 24.15.0で53ファイル・321テスト成功。`pnpm typecheck`、`pnpm lint`、`pnpm build`、`git diff --check`成功。追加箇所のlint整形エラーは既存ESLintの自動修正で解消した。
- 実ブラウザのIME・Canvas確認は未実施。疑似DOMで入力イベントから描画待機フラグまでの接続を検証している。
- 次の工程はサムネイル。読込要求・完了・リセットを制御する回帰テストを分割前に追加する。

### 2026-09-16: 工程2・分割前のテスト

- 画面からサムネイルを要求する10テストを追加。全331テストが成功した。
- 最大2並列、待機中・取得済みの重複防止、キャッシュなし・デコード失敗・寸法不正の再生成と保存を検証した。
- 読込失敗後のキュー継続・再試行、フォルダ変更・カード削除・画面終了後の遅延結果破棄、同じフォルダを開き直した際の世代管理を検証した。
- 検出力確認: 世代比較を一時的に外すと、同じフォルダを開き直すテストが古い結果の混入を検出して失敗した。比較を復元した。
- テストfixtureの型を実サービスの`File | null`・書込結果`boolean`に合わせて修正し、テストの型検査も成功した。

### 2026-09-16: 工程2・抽出

- `useCardThumbnails`へキュー、世代、キャッシュ読込・再生成・保存、終了時の無効化を移した。親は要求・リセット等の操作を接続する。
- runtimeにはサムネイルURLの所有と解放を残し、composableへ渡すruntimeの型を必要な4項目に限定した。
- 分割前と同じ17件の画面テストが成功。全331テスト、型検査、lint、ビルドも成功した。
- `CardEditor.vue`は3,834行（開始時から159行減）。次は翻訳の回帰テストと抽出。

### 2026-09-16: 工程3・翻訳候補の分割前テスト

- 翻訳候補の14テストを追加。送信確認・取消、適用とUndo/Redo、候補破棄、原文・訳文・領域・カード変更後の拒否、選択だけが変わった場合の適用先、二重要求防止、失敗後の再試行、機能無効時とデモを検証する。
- 検出力確認: 訳文の一致判定を一時的に外すと、手動編集を候補が上書きしてテストが失敗した。判定を復元した。
- 画面テストを描画遅延・サムネイル・翻訳の3ファイルに分け、共通のマウント・I/Oモック・fixtureを`helpers/card-editor.ts`へ整理した。
- テスト整理中のI/Oモック参照の不一致は、実サービスの型を持つhoistedモックを共有することで解消した。領域削除テストのイベント名は実際の`remove`に合わせた。
- 分割前の画面テスト31件とテスト型検査が成功した。レビュー・CSV連携のテストと分割は次の単位で扱う。

### 2026-09-16: 工程3・翻訳候補の抽出

- `useEditorTranslation`へ送信確認・非同期取得・候補の検証と適用・デモ翻訳を移した。ストアや履歴の所有は変更していない。
- 同じ31件の画面テスト、全345テスト、型検査、lint、ビルドが成功した。
- `CardEditor.vue`は3,719行（開始時から274行減）。次はレビュー・CSVの複数カード反映をテストする。

### 2026-09-16: 工程3・レビューとCSVの分割前テスト

- 保存済みプロジェクトを開く操作を経由する8テストを追加し、実ストアと編集履歴の同期を確認した。全353テストとテスト型検査が成功した。
- 選択行だけの反映、編集中カードのみのUndo/Redo、他カードへの反映保持、部分適用時のダイアログ維持、古いレビュー全体の拒否、削除予定の除外を検証した。
- CSVの照合前に編集を変えないこと、確認後の共通反映経路、全体・カード別CSV出力、レビュー画像読込と領域への移動も検証した。
- 検出力確認: レビューの最新状態チェックを一時的に外すと、原文変更・訳文変更・領域削除を検出する3テストが失敗した。チェックは復元済み。

### 2026-09-16: 工程3・レビューとCSVの抽出

- `useTranslationReview`へ確認用下書き・画像読込・領域への移動・CSV入出力・複数カードへの反映を移した。
- 同じ39件の画面テスト、全353テスト、型検査、lint、ビルドが成功した。
- `CardEditor.vue`は3,547行（開始時から446行減）。工程3完了。次はOCRの分割前テスト。

### 2026-09-16: 工程4・単一領域OCRの分割前テスト

- 11テストを追加。進捗、二重要求防止、候補の確定とUndo/Redo、手動修正・破棄、対象変更後の拒否、補正・辞書変更、空結果・失敗後の再試行、Worker終了を検証した。
- 分割前の全364テストとテスト型検査が成功した。
- 検出力確認: 認識中の本文一致判定を一時的に外すと、変更済み領域へ古い候補が表示されることをテストが検出した。判定は復元済み。
- 単一領域認識と一括認識の二重実行を防ぐため、実行中・進捗・状態の参照とOCR Providerを共有し、まず単一領域と辞書補正の責務を抽出する。

### 2026-09-16: 工程4・単一領域OCRの抽出

- `useEditorOCR`へ単一領域の認識・レイアウト・候補確認・辞書補正を移した。Workerと実行状態は全体・一括OCRと共有する。
- 同じ50件の画面テスト、全364テスト、型検査、lint、ビルドが成功した。
- `CardEditor.vue`は3,337行（開始時から656行減）。次は領域候補・一括OCRの境界を確認する。

### 2026-09-16: 一括OCRの既存不具合を検出して分割を一時停止

ユーザーの「気になる点が出るまで進める」という方針に従い、次の既存不具合を確認した段階で分割を止めた。

- 3カードの一括OCRで1枚目を処理中に画面を終了すると、Workerの`dispose`後にも2・3枚目の画像読込と`recognize`が開始される。
- 1枚目が終了後に成功する場合・失敗する場合の双方を、制御可能なPromiseで再現した。終了後のOCR総呼出数は期待1回に対して実際3回。
- 原因は画面終了時に一括キューの中止を要求していないこと。`runSequentialOCRQueue`は1枚の失敗後も次を処理する設計で、`TesseractOCRProvider.dispose`だけではキューは止まらない。Providerは次の認識でWorkerを再作成できる。
- 開始前の`3175fd1`の`CardEditor.vue`でも同じ2テストが失敗することを確認した。今回の分割による退行ではない。確認後はコミット済みの最新版を復元した。
- 再現テスト: `tests/components/CardEditor.batch-ocr.test.ts`。不具合修正前の赤いテストとして未コミットで保持する。skipや期待値の緩和は行っていない。
- 現在の作業ツリー全体では364テスト成功・新規再現2テスト失敗。コミット済みの分割は全364テスト・型検査・lint・ビルド成功済み。新規再現テストの型検査・lintも成功した。

再現コマンド:

```bash
mise exec -- pnpm test tests/components/CardEditor.batch-ocr.test.ts
```

推奨する次の作業は、分割と別の不具合修正コミットで終了時の一括処理を無効化すること。
終了後の次カード開始を防ぎ、遅延した進捗・結果・確認画面への移動も無効化した上で、上記2テストを緑にして分割を再開する。
実ブラウザのWorkerを使った再現は未実施。確認したのは画面ライフサイクル・キュー・Provider呼出の連携である。

### 2026-09-16: 承認された一括OCRの終了処理を修正

- ユーザーの承認を受け、分割とは別の不具合修正として対応した。
- 画面終了時に一括OCRを無効化して中止を要求する。画像読込・デコード・加工・認識の各待機後に終了を確認し、その先の処理・結果反映・通知・レビュー画面への移動を止める。
- 作成済みのBitmapは終了時も`finally`で解放する。通常の中止操作では処理中のカードを完了させ、候補を確認できる従来の動作を維持する。
- 元の再現2件に、画像読込・デコード・加工中の終了3件、通常中止で候補を保持する1件を加えた6テストが成功した。
- 全370テスト、型検査、lint、ビルドが成功。赤い再現テストは解消し、領域候補・一括OCRの分割を再開する。

### 2026-09-16: 工程4・一括OCRの分割前テスト

- 順次実行と一部失敗からの継続、候補確定とUndo、カード切替時の候補編集保持、破棄後の次カード確認、対象カードの絞り込みを3テストで追加した。
- 保存済みプロジェクトの画面テスト準備を共通化。一括OCR9テストを含む全373テスト、テスト型検査、lintが成功した。
- 検出力確認: 候補移動後のカード別保存を一時的に外すと、カード切替後の座標保持テストが失敗した。実装は復元済み。

### 2026-09-16: 工程4・一括OCRの抽出

- `useBatchOCR`へ順次認識、対象カードの選別、カード別結果と進捗、確認待ちカードへの移動、キャンセルと画面終了時の無効化を移した。
- Canvas上の候補編集とバッチ結果の受け渡しは親に残し、OCR Providerと実行状態は単一領域OCRと共有する。
- 同じ59件の画面テスト、全373テスト、型検査、lint、ビルドが成功した。
- `CardEditor.vue`は3,133行（開始時3,993行から860行減）。次は現在のカードの領域候補検出・編集を検証する。

### 2026-09-16: 現在のカードの領域検出にも終了後の処理継続を確認

- ユーザーの「気になる点が出るまで進める」方針に従い、次の分割前テストで停止した。一括OCRとは別の、現在の画像全体を認識する`detectRegionCandidates`が対象。
- 画像前処理中に画面終了すると、Providerの`dispose`後にも`recognize`を呼ぶ（期待0回、実際1回）。認識結果の成功・失敗が画面終了後に届く場合も、完了ログ・エラー通知の処理が続く。
- 制御可能なPromiseによる3テストで再現。分割開始前の`3175fd1`でも同じ3件が同じ理由で失敗することを確認し、最新版へ復元した。今回の抽出による退行ではない。実ブラウザのWorkerでは未検証。
- `tests/components/CardEditor.region-candidates.test.ts`に赤い再現テストを未コミットで保持。テスト型検査・lintは成功。コミット済みの一括OCR抽出は全373テスト・型検査・lint・ビルド成功済み。
- 次の対応案: 画像全体の領域検出にも終了時の無効化と各非同期処理後の確認を入れ、遅延した進捗・結果・通知を止める。分割とは別の修正コミットとして3件を緑にしてから候補編集の抽出へ進む。

再現コマンド:

```bash
mise exec -- pnpm test tests/components/CardEditor.region-candidates.test.ts
```

### 2026-09-16: 画像全体OCRの終了処理を修正

- 承認を受け、画面終了後の認識開始・進捗・結果・エラー通知を無効化した。
- 再現3テストを含む全376テスト、型検査、lint、ビルドが成功。候補検出・編集の分割を再開する。

### 2026-09-16: 工程4・領域候補の検出と編集を抽出

- 分割前に、候補の移動による行座標の変換、分割と候補用Undo、選択候補だけの確定と領域Undo、認識失敗後の再試行・破棄を2テスト追加。全378テストが成功した。
- `useRegionCandidates`に現在の画像の認識、候補の選択・移動・分割・履歴、終了時の無効化を移した。確定時の領域への反映とバッチ結果との受け渡しは親が担当する。
- 共通の候補複製処理はOCR候補サービスへ移し、一括OCRと候補編集から共有した。
- 抽出後も全378テスト、型検査、lint、ビルドが成功。`CardEditor.vue`は2,941行（開始時から1,052行減）。次はアセットとフォントの管理を検証する。

### 2026-09-16: アセット確定の非同期競合を確認し、分割を一時停止

- PNG生成（Canvasの`toBlob`）を待つ間に確定を2回押すと、同じ名前のアセットが別IDで2件登録される。生成待ちにキャンセルしても、その後アセットが登録される。
- 確定ボタンは名前のエラー時だけ無効になり、生成中の二重実行を防いでいない。親の処理も生成完了後に下書きの有効性を確認していない。
- `tests/components/CardEditor.assets.test.ts`で正常登録1件は成功、二重確定・キャンセルの2件は失敗。開始前の`3175fd1`でも同じ結果を確認し、最新版に復元した。今回の分割による退行ではない。
- テストではCanvasの描画を省き、PNG生成完了のコールバックだけを制御している。実ブラウザでの再現は未実施。テスト型検査とlintは成功した。
- ユーザーの「気になる点が出るまで」の方針に従い停止。再現テストとAssetEditorのpropsを補うテスト用stub変更は未コミットで保持する。コミット済みの抽出は全378テストと各検査が成功済み。
- 次の修正案: 実行中の二重確定を防ぎ、キャンセル・下書き変更・画像変更・画面終了後の古い生成結果を無効化する。修正を別コミットにし、再現テストを通してからアセット管理の分割を再開する。

```bash
mise exec -- pnpm test tests/components/CardEditor.assets.test.ts
```

### 2026-09-16: アセット確定の競合を修正・継続方針を更新

- ユーザーより、不具合は修正後に分割を再開し、都度確認せず進める方針を受領した。以後も不具合修正と責務抽出は別コミットにする。
- 確定中は二重実行を防ぎ、ボタンに生成中と表示。キャンセル・下書き変更・元画像変更・画面終了後の生成結果を破棄する。更新対象と名前は生成後にも再確認する。
- 新しい下書きの生成中に古い処理が完了しても、新しい処理の状態を消さない。生成失敗時は下書きを保持し再試行できる。
- 再現2件を含むアセット9テスト、全387テスト、型検査、lint、ビルドが成功。実ブラウザのCanvas描画は未検証。

### 2026-09-16: 工程4・アセット管理の抽出

- 再切り出しでのID・配置設定維持、削除時の画像と保存待ちデータの解放、重複名拒否を分割前テストへ追加した。
- `useEditorAssets`へ作成・再切り出し下書き、画像生成、共有定義とトークンの改名、配置設定、削除を移した。元画像の読込とruntimeによるリソース所有は維持する。
- 同じアセット11テストを含む全389テスト、型検査、lint、ビルドが成功。`CardEditor.vue`は2,782行。次はフォント管理を検証する。

### 2026-09-16: フォント読込の既存不具合を修正

- 分割前テストで、画面終了後に読込が完了するとフォント定義とruntimeへ登録されることを確認。開始前の`3175fd1`でも正常系3件成功・終了時1件失敗を確認した。
- 読込の世代を管理し、終了後・別ディレクトリやキャッシュ復元開始後の古い結果を破棄する。不要なFontFaceを解除し、古いキャッシュ処理の通知・状態更新も抑止する。
- 正常読込・再読込・キャッシュ失敗・使用中フォント削除・終了・ディレクトリ変更・キャッシュ待ち・OSフォント読込を7テストで検証。全396テスト、型検査、lint、ビルドが成功した。

### 2026-09-16: 工程4・フォント管理の抽出

- `useEditorFonts`へファイル・OS・キャッシュからの読込、世代管理、表示用family、使用中フォント削除の確認、共有参照解除、保存後のキャッシュ削除を移した。
- フォントの所有はruntime、永続データはストアに残し、親からは必要な参照と操作だけ渡す。
- 同じ7件の画面テスト、全396テスト、型検査、lint、ビルドが成功。`CardEditor.vue`は2,568行。次は工程4の残りの連携確認と工程5の保存・切替を検証する。

### 2026-09-16: 工程5・保存処理のテストと抽出

- 保存失敗時の編集・保存待ち画像・カード削除予定の保持、保存中の追加編集と画像差し替え、正常保存、初回保存後のUndo、切替先画像読込失敗時の現カード保持を5テストで検証した。
- 検出力確認: 保存待ち画像を一律に消す一時変更で、保存中の画像差し替え保持テストが失敗した。変更は復元済み。
- `useProjectPersistence`へ保存スナップショットの作成と成功時の承認を抽出。ストアとruntimeは必要な公開操作・データに型を限定して渡す。
- 全401テスト、型検査、lint、ビルドが成功。`CardEditor.vue`は2,480行。開く・切替・一括書き出しは未抽出。

- 工程4の補足確認: 2候補をまとめて確定し、1回のUndoで両方戻す画面テストを追加。候補6テスト・テスト型検査・lint成功。

### 2026-09-16: 書き出し処理の既存不具合を修正

- PNG/JPEGの一括書き出し、削除予定の除外、元カードへの復帰、空画像のスキップ、二重要求抑制をテストした。
- 画面終了後に単体・一括のダウンロードが始まる2件と、復帰時の通知が書き出し件数を上書きする1件が失敗した。終了後の結果を無効化し、件数通知を復帰完了後に出すよう修正した。
- 書き出し5テストを含む全407テスト、型検査、lint、ビルドが成功。Canvasの画像生成はモックし、ダウンロード呼出とカード遷移を検証している。

### 2026-09-16: 工程5・画像書き出しの抽出

- `useCardImageExport`へ単体・一括書き出しと元カードへの復帰、終了後の無効化を移した。画像生成はCanvas、カード切替は親から渡す操作に委ねる。
- 同じ5件を含む全407テスト、型検査、lint、ビルドが成功。`CardEditor.vue`は2,423行。
